/* Made by Ernesto Lang 3/31/2015 */

/* A sequence is created to be entered onto person_id, every time an 
insertion is made, the trigger below is in charge of that */

CREATE SEQUENCE person_id_sequence;

CREATE OR REPLACE TRIGGER person_id_generator
BEFORE INSERT ON Person
FOR EACH ROW 
  BEGIN 
     SELECT person_id_sequence.NEXTVAL
     INTO   :new.person_id
     FROM   dual;
   END;
   

   
